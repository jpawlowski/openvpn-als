<?xml version="1.0" encoding="UTF-8"?>
<project name="maverick-util" default="dist">

	<!-- This is no longer needed after mavenization has been finished. Targets do nothing. -->

	<!-- Clean -->
	<target name="clean-dist" />

	<!-- Build the library jar files -->
	<target name="dist" />

	<!-- Compile the library  -->
	<target name="compile" />

	<!-- Clean up all built files -->
	<target name="clean" />

	<!-- Create API documentation -->
	<target name="javadoc" />

</project>
